manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: caksoylar
      url-base: https://github.com/caksoylar
    - name: geeksville
      url-base: https://github.com/geeksville
    - name: beekeeb
      url-base: https://github.com/beekeeb
    - name: albertclee
      url-base: https://github.com/alee0729
  projects:
    - name: zmk
      remote: zmkfirmware
      import: app/west.yml
      revision: v0.3
    - name: zmk_driver_azoteq
      remote: albertclee
      # TEMP: pinned to the feature branch so this PR's CI compiles the new
      # idle-scan-rate-ms driver code (the driver repo has no CI of its own).
      # Revert to `toucan2` once the driver PR merges there.
      revision: slow-scan-idle
    - name: zmk-rgbled-widget
      remote: caksoylar
      revision: v0.3
    - name: zmk-input-zoom
      remote: beekeeb
      revision: main
  self:
    path: config
